<h2>Creatures of the Bog</h2>


<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <% @creatures.each do |c| %>

      <li><%= link_to c.name, c %></li>

    <% end %>
  </div>
</div>

<%= link_to "Add a new Creature", new_creature_path, :class=>"btn btn-primary" %>
